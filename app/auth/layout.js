export const metadata = {
  title: 'Connexion / Inscription — HookGenerator',
  description: 'Crée ton compte gratuit et génère tes premiers hooks viraux en quelques secondes. Sans carte bancaire.',
  alternates: { canonical: 'https://hookgenerator.eu/auth' },
  robots: { index: false, follow: false },
};

export default function AuthLayout({ children }) {
  return children;
}